#!/bin/bash
echo "🔧 إصلاح المشاكل وتشغيل الموقع..."

# إيقاف أي عملية تعمل على المنفذ 3000
lsof -ti:3000 | xargs kill -9 2>/dev/null || true

# حذف .next للبدء من جديد
rm -rf .next

# تشغيل الخادم
echo "🚀 تشغيل الخادم..."
npm run dev
