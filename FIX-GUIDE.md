# دليل إصلاح المشاكل

## ✅ تم إصلاح المشاكل التالية:

1. ✅ إضافة `shadow-gold` إلى Tailwind config
2. ✅ استبدال CSS classes المخصصة بـ Tailwind classes مباشرة
3. ✅ تحديث جميع الألوان لاستخدام `asawer-primary-500`, `asawer-secondary-500`, `asawer-accent-500`

## 🚀 لتشغيل الموقع:

### الطريقة 1: من Terminal

```bash
cd /Users/mac/asawer-real-estate-evaluation/asawer-website
rm -rf .next
npm run dev
```

### الطريقة 2: إذا كان هناك خطأ

```bash
# 1. احذف مجلد .next
rm -rf .next

# 2. أعد تثبيت الحزم (إذا لزم الأمر)
npm install

# 3. شغّل المشروع
npm run dev
```

## 🔍 المشاكل الشائعة وحلولها:

### 1. خطأ في الألوان (asawer-primary-500 not found)
**الحل:** تم إصلاحه - الألوان معرّفة في `tailwind.config.ts`

### 2. خطأ في CSS classes (btn-luxury not found)
**الحل:** تم استبدالها بـ Tailwind classes مباشرة

### 3. الخادم لا يبدأ
**الحل:**
```bash
# تأكد من أن المنفذ 3000 غير مستخدم
lsof -ti:3000 | xargs kill -9

# ثم شغّل مرة أخرى
npm run dev
```

### 4. خطأ في next-intl
**الحل:** تأكد من وجود ملفات الترجمة في `messages/ar.json` و `messages/en.json`

## 📝 ملاحظات:

- الألوان الجديدة: Navy Blue (#0A2540) + Gold (#D4AF37)
- التصميم مستوحى من: إعمار، داماك، إلينغتون
- جميع المكونات محدّثة

## 🌐 افتح المتصفح على:

**http://localhost:3000/ar**

أو

**http://localhost:3000/en**

---

إذا استمرت المشكلة، أرسل رسالة الخطأ الكاملة.

