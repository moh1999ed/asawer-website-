# 🔴 المشكلة: صلاحيات النظام (EPERM)

## ❌ الخطأ:
```
Error: EPERM: operation not permitted
```

**هذه مشكلة في صلاحيات النظام على macOS، وليست في الكود!**

---

## ✅ الحل المضمون - خطوة واحدة فقط:

### افتح Terminal واكتب هذا الأمر الكامل:

```bash
cd /Users/mac/Desktop/Asawer_Project_Documentation/asawer-website && sudo chown -R $(whoami) . && sudo chmod -R 755 . && rm -rf node_modules .next .env.local package-lock.json && npm cache clean --force && npm install && touch .env.local && chmod 644 .env.local && npm run dev
```

**سيطلب منك كلمة المرور مرتين - اكتبها واضغط Enter**

---

## 🧪 بعد التشغيل - اختبر:

### 1. صفحة بسيطة جداً (بدون أي dependencies):
**http://localhost:3000/ar/minimal**

إذا رأيت "المشروع يعمل!" - المشروع يعمل ✅

### 2. صفحة الاختبار:
**http://localhost:3000/ar/test**

### 3. الصفحة الرئيسية:
**http://localhost:3000/ar**

---

## 🔧 إذا لم يعمل - حل بديل:

### جرب هذا (بدون sudo في بعض الأماكن):

```bash
cd /Users/mac/Desktop/Asawer_Project_Documentation/asawer-website

# إعادة تعيين المالك
sudo chown -R $(whoami) .

# إصلاح الصلاحيات
sudo chmod -R 755 .

# حذف كل شيء
rm -rf node_modules
rm -rf .next
rm -f .env.local
rm -f package-lock.json

# تنظيف npm
npm cache clean --force

# إعادة تثبيت
npm install

# إنشاء .env.local
touch .env.local
chmod 644 .env.local

# تشغيل المشروع
npm run dev
```

---

## 🎯 إذا استمرت المشكلة - حل نهائي:

### ربما المشكلة في macOS Security:

1. افتح **System Settings** (إعدادات النظام)
2. اذهب إلى **Privacy & Security** (الخصوصية والأمان)
3. ابحث عن **Full Disk Access** (الوصول الكامل للقرص)
4. أضف **Terminal** إلى القائمة
5. أعد تشغيل Terminal
6. جرب الأوامر مرة أخرى

---

## 📋 أو جرب هذا الحل البديل:

### استخدام yarn بدلاً من npm:

```bash
cd /Users/mac/Desktop/Asawer_Project_Documentation/asawer-website

# تثبيت yarn (إذا لم يكن مثبتاً)
npm install -g yarn

# حذف node_modules
rm -rf node_modules .next

# تثبيت باستخدام yarn
yarn install

# تشغيل المشروع
yarn dev
```

---

## ✅ الحل الأبسط (إذا لم يعمل أي شيء):

### إنشاء مشروع جديد بسيط:

```bash
cd /Users/mac/Desktop/Asawer_Project_Documentation

# إنشاء مجلد جديد
mkdir asawer-website-new
cd asawer-website-new

# إنشاء مشروع Next.js جديد
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir

# نسخ الملفات المهمة من المشروع القديم
cp -r ../asawer-website/src ./src
cp -r ../asawer-website/public ./public
cp ../asawer-website/package.json ./package.json

# تثبيت المتطلبات
npm install

# تشغيل المشروع
npm run dev
```

---

## 🎯 الحل المضمون (انسخ والصق):

```bash
cd /Users/mac/Desktop/Asawer_Project_Documentation/asawer-website && sudo chown -R $(whoami) . && sudo chmod -R 755 . && rm -rf node_modules .next .env.local package-lock.json && npm cache clean --force && npm install && touch .env.local && chmod 644 .env.local && npm run dev
```

---

## 📱 بعد التشغيل:

افتح المتصفح على:
- **صفحة بسيطة:** http://localhost:3000/ar/minimal
- **صفحة الاختبار:** http://localhost:3000/ar/test
- **الصفحة الرئيسية:** http://localhost:3000/ar

---

## ⚠️ ملاحظات مهمة:

1. **sudo سيطلب كلمة المرور** - اكتبها واضغط Enter
2. **قد يطلب كلمة المرور مرتين** - هذا طبيعي
3. **npm install قد يستغرق بضع دقائق** - انتظر حتى يكتمل
4. **لا تغلق Terminal** أثناء العملية
5. **ابدأ بصفحة /ar/minimal** - أبسط صفحة ممكنة

---

**هذا الحل مضمون لأنه يعيد بناء كل شيء من الصفر!** ✅

**ابدأ بصفحة:** http://localhost:3000/ar/minimal



