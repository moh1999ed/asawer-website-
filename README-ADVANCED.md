# موقع أساور للتطوير العقاري - الإصدار المتقدم

موقع إلكتروني فاخر لشركة أساور للتطوير العقاري مع نظام توزيع العملاء الذكي.

## المميزات الرئيسية

### 🎨 التصميم
- **Minimalist Luxury** - تصميم فاخر بسيط وأنيق
- **لوحة ألوان:** التركوازي، الأزرق الجميل، الأبيض السحابي، والرمادي
- **حركات Framer Motion** - حركات انسيابية وفاخرة
- **Hero Video** - فيديو خلفية بدقة 4K

### 🌍 دعم ثنائي اللغة
- **عربي/إنجليزي** - تبديل سلس بين اللغات
- **RTL/LTR** - دعم كامل للاتجاهات
- **next-intl** - نظام ترجمة متقدم

### 🤖 نظام توزيع العملاء (Lead Distribution)
- **توزيع عشوائي** - توزيع تلقائي على 7 موظفين
- **مؤقت 5 دقائق** - إعادة توزيع تلقائية إذا لم يتم القبول
- **تكامل Supabase** - قاعدة بيانات قوية وآمنة
- **إشعارات للموظفين** - تنبيهات فورية للعملاء الجدد

### 📍 خرائط تفاعلية
- **Google Maps API** - خرائط مخصصة للمشاريع
- **توزيع جغرافي** - عرض مواقع المشاريع على الخريطة

### 🎥 محتوى متقدم
- **جولات افتراضية 360°** - دعم Matterport و Three.js
- **معرض صور وفيديوهات** - عرض احترافي للمشاريع
- **فلاتر ذكية** - فلترة المشاريع حسب النوع والمنطقة

## التقنيات المستخدمة

- **Next.js 15.5.6** - App Router
- **TypeScript** - للكتابة الآمنة
- **Tailwind CSS 4** - للتصميم
- **Framer Motion** - للحركات
- **Supabase** - قاعدة البيانات
- **next-intl** - الترجمة
- **Google Maps API** - الخرائط
- **React Player** - الفيديوهات

## التثبيت

### 1. تثبيت المتطلبات

```bash
npm install
```

### 2. إعداد المتغيرات البيئية

أنشئ ملف `.env.local` واملأه:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key
WHATSAPP_PHONE_NUMBER=96893994710
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 3. إعداد قاعدة البيانات Supabase

1. أنشئ مشروع جديد على [Supabase](https://supabase.com)
2. نفذ ملف `src/lib/database-schema.sql` في SQL Editor
3. أضف 7 موظفين في جدول `agents`:

```sql
INSERT INTO agents (name, email, phone, whatsapp_link, is_active) VALUES
('موظف 1', 'agent1@asawer.om', '968XXXXXXXX', 'https://wa.me/968XXXXXXXX', true),
('موظف 2', 'agent2@asawer.om', '968XXXXXXXX', 'https://wa.me/968XXXXXXXX', true),
-- ... إلخ
```

### 4. تشغيل المشروع

```bash
npm run dev
```

## هيكل المشروع

```
asawer-website/
├── src/
│   ├── app/
│   │   ├── [locale]/          # صفحات متعددة اللغات
│   │   │   ├── page.tsx       # الصفحة الرئيسية
│   │   │   ├── projects/      # صفحة المشاريع
│   │   │   └── projects/[id]/ # صفحة تفاصيل المشروع
│   │   ├── api/
│   │   │   ├── leads/         # API للعملاء
│   │   │   └── agents/        # API للموظفين
│   │   └── layout.tsx
│   ├── components/
│   │   ├── Header.tsx
│   │   ├── Footer.tsx
│   │   ├── HeroSection.tsx
│   │   ├── LeadForm.tsx       # نموذج تسجيل الاهتمام
│   │   └── ...
│   ├── lib/
│   │   ├── supabase.ts        # إعداد Supabase
│   │   ├── lead-distribution.ts # نظام التوزيع
│   │   └── database-schema.sql
│   └── i18n/                  # نظام الترجمة
├── messages/
│   ├── ar.json
│   └── en.json
└── package.json
```

## نظام توزيع العملاء

### كيف يعمل:

1. **الالتقاط:** العميل يملأ النموذج → يتم حفظه في `leads`
2. **التوزيع:** النظام يختار موظفاً عشوائياً من 7 موظفين نشطين
3. **المؤقت:** يتم تفعيل مؤقت 5 دقائق
4. **القبول:** الموظف يضغط "قبول" → يتم ربطه بالعميل
5. **الإعادة:** إذا انتهت 5 دقائق بدون قبول → يتم إعادة التوزيع

### API Endpoints:

- `POST /api/leads` - إنشاء عميل جديد
- `POST /api/agents/claim` - قبول عميل من قبل موظف
- `GET /api/leads/check-timeout` - فحص العملاء المنتهية صلاحيتهم

### Cron Job:

يجب إعداد cron job لاستدعاء `/api/leads/check-timeout` كل دقيقة:

```bash
* * * * * curl https://your-domain.com/api/leads/check-timeout
```

## النشر

### Vercel (موصى به):

1. ارفع المشروع على GitHub
2. اربطه بـ Vercel
3. أضف المتغيرات البيئية
4. Deploy!

### إعداد Cron Job على Vercel:

استخدم Vercel Cron Jobs في `vercel.json`:

```json
{
  "crons": [{
    "path": "/api/leads/check-timeout",
    "schedule": "* * * * *"
  }]
}
```

## الدعم

للمساعدة أو الاستفسارات:
- **البريد:** info@asawer.om
- **الهاتف:** 93994710 - 8009008

---

**تم التطوير بواسطة:** فريق تطوير أساور  
**الإصدار:** 2.0.0

