# 🔧 حل المشكلة - خطوات واضحة

## المشكلة:
```
Error: EPERM: operation not permitted
```

هذه مشكلة في **صلاحيات النظام**، وليست في الكود.

---

## ✅ الحل السريع:

### افتح Terminal واكتب هذه الأوامر بالترتيب:

```bash
# 1. الانتقال إلى مجلد المشروع
cd /Users/mac/Desktop/Asawer_Project_Documentation/asawer-website

# 2. إصلاح صلاحيات الملفات
chmod -R 755 .

# 3. إصلاح صلاحيات node_modules
chmod -R 755 node_modules

# 4. إنشاء ملف .env.local (إذا كان مفقوداً)
touch .env.local

# 5. إعادة تثبيت GSAP (إذا لم يكن مثبتاً)
npm install gsap

# 6. تشغيل المشروع
npm run dev
```

---

## 🎯 إذا لم يعمل الحل السريع:

### جرب هذا:

```bash
cd /Users/mac/Desktop/Asawer_Project_Documentation/asawer-website

# إعادة تعيين المالك
sudo chown -R $(whoami) .

# إصلاح الصلاحيات
chmod -R 755 .

# إعادة تثبيت node_modules
rm -rf node_modules
npm install

# تثبيت GSAP
npm install gsap

# تشغيل المشروع
npm run dev
```

---

## 📱 بعد التشغيل:

افتح المتصفح على:
- **العربية:** http://localhost:3000/ar
- **English:** http://localhost:3000/en

---

## ⚠️ ملاحظات مهمة:

1. **لا تستخدم sudo** إلا إذا كان ضرورياً
2. **تأكد من أنك في المجلد الصحيح** قبل تشغيل الأوامر
3. **انتظر حتى يكتمل** `npm install` قبل تشغيل `npm run dev`

---

## 🔍 التحقق من أن كل شيء يعمل:

```bash
# التحقق من Node.js
node --version

# التحقق من npm
npm --version

# التحقق من GSAP
npm list gsap
```

---

## 📞 إذا استمرت المشكلة:

1. أعد تشغيل Terminal
2. تأكد من أن Node.js مثبت بشكل صحيح
3. جرب إعادة تشغيل الكمبيوتر

---

**بعد تطبيق هذه الخطوات، المشروع سيعمل!** ✅



