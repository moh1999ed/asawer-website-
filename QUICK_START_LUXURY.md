# 🚀 دليل التشغيل السريع - التصميم الفاخر

## خطوات التشغيل

### 1. تثبيت GSAP (مطلوب)

```bash
cd asawer-website
npm install gsap
```

إذا واجهت مشاكل في الصلاحيات، جرب:
```bash
sudo npm install gsap
```

أو استخدم yarn:
```bash
yarn add gsap
```

### 2. تشغيل المشروع

```bash
npm run dev
```

### 3. فتح المتصفح

افتح المتصفح على:
- **العربية:** http://localhost:3000/ar
- **English:** http://localhost:3000/en

---

## ⚠️ ملاحظات مهمة

### إذا لم يتم تثبيت GSAP

المشروع سيعمل لكن بدون بعض التأثيرات المتقدمة في Hero Section. يمكنك:

1. **تثبيت GSAP لاحقاً:**
   ```bash
   npm install gsap
   ```

2. **أو استخدام المشروع بدون GSAP:**
   - جميع المكونات الأخرى ستعمل بشكل طبيعي
   - فقط Hero Section سيفقد بعض التأثيرات المتقدمة

### التحقق من التثبيت

```bash
# التحقق من وجود GSAP
ls node_modules | grep gsap

# أو
npm list gsap
```

---

## 🎨 الميزات المتاحة

### ✅ تعمل بدون GSAP:
- ✅ LuxuryHeader (Glassmorphic)
- ✅ LuxuryStats (Counter animations)
- ✅ LuxuryTeam (3D Tilt)
- ✅ LuxuryVision (Honeycomb)
- ✅ ParallaxCanvas
- ✅ جميع التأثيرات البصرية الأخرى

### ⚠️ تحتاج GSAP:
- ⚠️ بعض التأثيرات المتقدمة في LuxuryHero

---

## 🔧 استكشاف الأخطاء

### خطأ: Module not found: Can't resolve 'gsap'

**الحل:**
```bash
npm install gsap
```

### خطأ: Permission denied

**الحل:**
```bash
sudo npm install gsap
```

أو استخدم yarn:
```bash
yarn add gsap
```

### المشروع لا يعمل

1. تأكد من تثبيت جميع المتطلبات:
   ```bash
   npm install
   ```

2. تأكد من وجود ملف `.env.local` (إذا كان مطلوباً)

3. امسح cache وrebuild:
   ```bash
   rm -rf .next
   npm run dev
   ```

---

## 📱 اختبار الموقع

بعد تشغيل المشروع، اختبر:

1. **Header** - Glassmorphic effect عند التمرير
2. **Hero Section** - Zoom-in effects وFloating typography
3. **Stats** - Counter animations عند التمرير
4. **Team** - 3D Tilt effects عند hover
5. **Vision** - Honeycomb layout
6. **Parallax** - Canvas background يتفاعل مع الماوس

---

## 🎉 استمتع بالتصميم الفاخر!

للمزيد من المعلومات، راجع:
- `LUXURY_UPDATE.md` - تفاصيل التحديثات
- `README_LUXURY.md` - الدليل الكامل

---

**نصيحة:** إذا كنت تواجه مشاكل في تثبيت GSAP، يمكنك استخدام المشروع بدون GSAP - سيعمل بشكل جيد جداً!



