# 🔍 الاشكالية والحل النهائي

## ❌ الاشكالية:

المشروع لا يعمل بسبب **مشكلة في صلاحيات النظام**:

```
Error: EPERM: operation not permitted
- على ملف .env.local
- على node_modules/next/...
```

---

## ✅ الحل السريع:

### افتح Terminal واكتب:

```bash
cd /Users/mac/Desktop/Asawer_Project_Documentation/asawer-website

# تشغيل سكريبت الإصلاح
bash fix-permissions.sh
```

أو يدوياً:

```bash
cd /Users/mac/Desktop/Asawer_Project_Documentation/asawer-website

# 1. إعادة تعيين المالك
sudo chown -R $(whoami) .

# 2. إصلاح الصلاحيات
chmod -R 755 .

# 3. إصلاح node_modules
chmod -R 755 node_modules

# 4. إنشاء/إصلاح .env.local
touch .env.local
chmod 644 .env.local
```

---

## 🚀 بعد إصلاح الصلاحيات:

```bash
# تثبيت GSAP (اختياري)
npm install gsap

# تشغيل المشروع
npm run dev
```

---

## 📱 افتح المتصفح على:

- **العربية:** http://localhost:3000/ar
- **English:** http://localhost:3000/en

---

## 🔧 إذا استمرت المشكلة:

### الحل الشامل:

```bash
cd /Users/mac/Desktop/Asawer_Project_Documentation/asawer-website

# حذف وإعادة تثبيت
rm -rf node_modules .next .env.local
npm install
npm install gsap
npm run dev
```

---

## 📋 ملخص:

| المشكلة | الحل |
|---------|------|
| صلاحيات .env.local | `chmod 644 .env.local` |
| صلاحيات node_modules | `chmod -R 755 node_modules` |
| صلاحيات عامة | `sudo chown -R $(whoami) .` |

---

**بعد تطبيق هذه الخطوات، المشروع سيعمل!** ✅



